package com.kadirsensoy.utils;

public class ConfigProgramming {
    //DevTools => LiveReload
    // File =>> Settings =>> Compiler =>> Build Project out comatically checked
    // File =>> Settings =>> Advenced Settings =>> Compiler =>> Allow outo-make to start even if develloped application is currentyl running (selected)
}
